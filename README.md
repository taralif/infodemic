cd ~/Claude/Projects/Infodemic/repo
git init
git add .
git commit -m "Initial commit: paper prototype, Feed Drop #1, curation charter, board"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/infodemic.git
git pull origin main --allow-unrelated-histories --no-edit
git push -u origin main
